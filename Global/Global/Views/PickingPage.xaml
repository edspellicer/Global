<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Global.Views.PickingPage"
             Title="Picking"
             BackgroundColor="{StaticResource BackgroundColor}"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">
    
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Entry Placeholder="Cantidad de Cajas"/>
                <Entry Placeholder="Ubicación"/>
                <Entry Placeholder="Código de Barras"/>
                <Entry Placeholder="Lote"/>
                <Entry Placeholder="Unidades Solicitadas"/>
                <Entry Placeholder="Unidades Disponibles"/>
                <Button
                    Text="Registrar"
                    BackgroundColor="DarkOliveGreen"
                    TextColor="Accent">
                    
                </Button>

            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
    
</ContentPage>